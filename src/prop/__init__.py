"""Learned transport inside CKF track finding.

Every module here is dual-purpose: it exports functions the other modules
import, and it has its own argparse entry point. Run one with

    python -m prop.<module> --help

from the repository root, because the file paths in the argparse defaults are
relative to the working directory.
"""
